# Development / CI dependencies for hivemind
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

# Test runner
pytest==8.3.5
pytest-asyncio==0.25.3
pytest-cov==6.1.0
anyio[trio]==4.12.1          # async test back-end

# HTTP test client (used in test_api.py)
httpx==0.28.1

# Linting + formatting
ruff==0.9.10

# Type-checking
mypy==1.15.0

# Coverage upload (optional local use)
coverage[toml]==7.8.0
